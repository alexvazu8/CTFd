services:
  ctfd:
    build: .
    user: root
    restart: always
    ports:
      - "8000:8000"
    environment:
      - UPLOAD_FOLDER=/var/uploads
      - DATABASE_URL=postgresql://ctfd_60yr_user:67Q330UwlNbByFZ1ND3ukFHY5zGteA8z@dpg-d2ev9iqdbo4c738tr8pg-a.oregon-postgres.render.com:5432/ctfd_60yr
      - SECRET_KEY=c1e3b2d8f4a9e7b6c5d8e3f2a1b4c7d8
      - REDIS_URL=redis://cache:6379
      - WORKERS=4
      - LOG_FOLDER=/var/log/CTFd
      - ACCESS_LOG=-
      - ERROR_LOG=-
      - REVERSE_PROXY=true
      # Configuración FTP para uploads
      - UPLOAD_PROVIDER=ftp
      - FTP_ENDPOINT=ctf.visionmundo.com
      - FTP_USERNAME=ctfd.visionmundo.com  # Reemplaza con tu usuario FTP
      - FTP_PASSWORD=70?G6;o7lf(3  # Reemplaza con tu contraseña FTP
      - FTP_PORT=21
      - FTP_PATH=/  # Ruta en el servidor FTP donde se guardarán los archivos
      - FTP_TLS=false  # Cambia a true si usas FTPS
      - AWS_S3_ENDPOINT=ctf.visionmundo.com  # Aunque dice AWS, CTFd lo usa para el dominio base 
    volumes:
      - .data/CTFd/logs:/var/log/CTFd
      - .data/CTFd/uploads:/var/uploads
      - .:/opt/CTFd:ro
    #depends_on:
      #- db
    networks:
        default:
        internal:

  nginx:
    image: nginx:stable
    restart: always
    volumes:
      - ./conf/nginx/http.conf:/etc/nginx/nginx.conf
    ports:
      - 80:80
    #depends_on:
     # - ctfd

# db:                        # ⚠️¡Eliminar o comentar!
#   image: mariadb:10.11     # No se usa si la BD está en Render/cloud
#   environment:
#     - MARIADB_ROOT_PASSWORD=ctfd
#     - MARIADB_USER=ctfd
#     - MARIADB_PASSWORD=ctfd
#     - MARIADB_DATABASE=ctfd
#   volumes:
#     - .data/mysql:/var/lib/mysql  # ¡También elimina este volumen!
    networks:
        internal:
    # This command is required to set important mariadb defaults
    #command: [mysqld, --character-set-server=utf8mb4, --collation-server=utf8mb4_unicode_ci, --wait_timeout=28800, --log-warnings=0]

  cache:
    image: redis:4
    restart: always
    volumes:
    - .data/redis:/data
    networks:
        internal:

networks:
    default:
    internal:
        internal: true
